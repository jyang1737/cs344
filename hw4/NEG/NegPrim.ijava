NegPrim
%%%
    public Val apply(Val [] vals) {
        if (vals.length != 1)
             throw new RuntimeException("One argument expected.");
        int i = vals[0].intVal();
        return new IntVal(-i);
    }

    public String toString() {
        return "neg";
    }
%%%